# Тестирование системы SpeechRecognition

Данный документ описывает подход к тестированию системы автоматического распознавания речи, используемые методики и рекомендации для создания и выполнения тестов.

## Содержание

- [Обзор стратегии тестирования](#обзор-стратегии-тестирования)
- [Типы тестов](#типы-тестов)
  - [Модульные тесты](#модульные-тесты)
  - [Интеграционные тесты](#интеграционные-тесты)
  - [Тесты восстановления](#тесты-восстановления)
  - [Тесты производительности](#тесты-производительности)
- [Инструменты тестирования](#инструменты-тестирования)
- [Запуск тестов](#запуск-тестов)
  - [Запуск всех тестов](#запуск-всех-тестов)
  - [Запуск отдельной категории тестов](#запуск-отдельной-категории-тестов)
  - [Настройка параметров тестирования](#настройка-параметров-тестирования)
- [Структура тестового проекта](#структура-тестового-проекта)
- [Чек-листы тестирования](#чек-листы-тестирования)
- [Тестовые кейсы](#тестовые-кейсы)
- [Покрытие кода тестами](#покрытие-кода-тестами)
- [Руководство по добавлению новых тестов](#руководство-по-добавлению-новых-тестов)
- [Мок-объекты и заглушки](#мок-объекты-и-заглушки)
- [CI/CD и автоматизация тестирования](#cicd-и-автоматизация-тестирования)

## Обзор стратегии тестирования

Стратегия тестирования системы SpeechRecognition направлена на обеспечение высокого качества и надежности распознавания речи. Тестирование охватывает все основные компоненты системы, начиная от обработки аудио и заканчивая распознаванием речи и управлением очередью.

**Основные принципы стратегии тестирования:**

1. **Многоуровневое тестирование** - от модульных тестов отдельных компонентов до интеграционного тестирования всей системы.
2. **Автоматизация** - максимальное количество тестов должно выполняться автоматически.
3. **Изоляция** - компоненты тестируются изолированно с использованием моков и заглушек.
4. **Регрессионное тестирование** - регулярное выполнение всех тестов для предотвращения регрессий.
5. **Тестирование восстановления** - проверка способности системы восстанавливаться после сбоев.
6. **Тестирование производительности** - оценка производительности распознавания речи.

## Типы тестов

### Модульные тесты

Модульные тесты проверяют функциональность отдельных компонентов системы в изоляции от других частей. Это обеспечивает быстрое обнаружение ошибок в конкретных компонентах.

**Основные модульные тесты:**

- **AudioProcessorTests** - тесты для компонента обработки аудио
- **RecognizerTests** - тесты для распознавателей речи
- **ModelExtensionTests** - тесты для расширений моделей
- **SessionManagerTests** - тесты для менеджера сессий

**Пример модульного теста:**

```csharp
[Fact]
public async Task AddWavHeader_AddsCorrectHeaderToPcmData()
{
    // Arrange
    var audioProcessor = new AudioProcessor();
    byte[] pcmData = GenerateFakePcmData(1000);
    
    // Act
    byte[] wavData = audioProcessor.AddWavHeader(pcmData);
    
    // Assert
    Assert.True(audioProcessor.IsWavFormat(wavData));
    Assert.Equal(pcmData.Length + 44, wavData.Length); // WAV заголовок = 44 байта
}
```

### Интеграционные тесты

Интеграционные тесты проверяют взаимодействие между различными компонентами системы. Они обеспечивают корректность работы системы как единого целого.

**Основные интеграционные тесты:**

- **SpeechRecognitionServiceTests** - тесты для сервиса распознавания речи
- **IntegrationTests** - общие интеграционные тесты системы

**Пример интеграционного теста:**

```csharp
[Fact]
public async Task ProcessFileAsync_ValidFile_ReturnsRecognizedText()
{
    // Пропускаем тест, если нет аудиофайла или модели
    if (!_hasTestWavFile || !_hasModel || _service == null)
    {
        return;
    }
    
    // Arrange
    await _service.InitializeAsync();
    
    // Act
    string result = await _service.ProcessFileAsync(_testWavPath!);
    
    // Assert
    Assert.NotNull(result);
    Assert.NotEmpty(result);
}
```

### Тесты восстановления

Тесты восстановления проверяют способность системы восстанавливаться после ошибок и сбоев. Они гарантируют надежность системы в реальных условиях эксплуатации.

**Основные тесты восстановления:**

- **RecoveryTests** - тесты политик восстановления и повторных попыток

**Пример теста восстановления:**

```csharp
[Fact]
public async Task RetryPolicy_RetriesToExecuteOperation()
{
    // Arrange
    int attemptsCount = 0;
    var policy = RetryPolicyFactory.CreateConstantBackoff(
        maxRetryCount: 3,
        delay: TimeSpan.FromMilliseconds(10)
    );
    
    // Act & Assert
    await Assert.ThrowsAsync<InvalidOperationException>(async () =>
    {
        await policy.ExecuteAsync(cancellationToken =>
        {
            attemptsCount++;
            throw new InvalidOperationException("Test exception");
        });
    });
    
    // Проверяем, что было выполнено 4 попытки (1 основная + 3 повторных)
    Assert.Equal(4, attemptsCount);
}
```

### Тесты производительности

Тесты производительности оценивают скорость и эффективность распознавания речи. Они помогают определить узкие места и оптимизировать производительность системы.

**Основные тесты производительности:**

- Тесты скорости распознавания речи
- Тесты параллельной обработки аудиофрагментов
- Тесты масштабируемости

**Пример теста производительности:**

```csharp
[Fact]
public async Task ProcessMultipleStreamsInParallel_CompletesWithinTimeLimit()
{
    // Пропускаем тест, если нет аудиофайла или модели
    if (!_hasTestWavFile || !_hasModel || _service == null)
    {
        return;
    }
    
    // Arrange
    await _service.InitializeAsync();
    var stopwatch = new Stopwatch();
    var audioChunks = CreateMultipleAudioChunks(5);
    
    // Act
    stopwatch.Start();
    var tasks = audioChunks.Select(chunk => _service.ProcessStreamAsync(chunk));
    await Task.WhenAll(tasks);
    stopwatch.Stop();
    
    // Assert
    Assert.True(stopwatch.Elapsed < TimeSpan.FromSeconds(30), 
        $"Параллельная обработка заняла слишком много времени: {stopwatch.Elapsed}");
}
```

## Инструменты тестирования

Для тестирования системы SpeechRecognition используются следующие инструменты:

1. **xUnit** - фреймворк для модульного тестирования
2. **Moq** - библиотека для создания мок-объектов
3. **FluentAssertions** - библиотека для создания читаемых утверждений
4. **BenchmarkDotNet** - инструмент для проведения микробенчмарков
5. **coverlet** - инструмент для измерения покрытия кода тестами

## Запуск тестов

### Запуск всех тестов

Для запуска всех тестов используйте команду:

```
dotnet test
```

### Запуск отдельной категории тестов

Для запуска определенной категории тестов:

```
dotnet test --filter "Category=Integration"
```

Доступные категории:
- Unit
- Integration
- Recovery
- Performance

### Настройка параметров тестирования

Тесты могут быть настроены через файл конфигурации `testsettings.json`:

```json
{
  "TestSettings": {
    "ModelPath": "models/ggml-tiny.bin",
    "AudioExamplesPath": "audioExamples",
    "Language": "ru",
    "PerformanceTestTimeoutSeconds": 30
  }
}
```

## Структура тестового проекта

```
SpeechRecognition.Tests/
├── AudioProcessorTests.cs     # Тесты обработки аудио
├── RecognizerTests.cs         # Тесты распознавателей речи
├── IntegrationTests.cs        # Интеграционные тесты
├── ModelExtensionTests.cs     # Тесты расширений моделей
├── RecoveryTests.cs           # Тесты восстановления
├── SessionManagerTests.cs     # Тесты менеджера сессий
├── SpeechRecognitionServiceTests.cs # Тесты сервиса распознавания
├── Recovery/                  # Дополнительные тесты восстановления
├── src/                       # Вспомогательные классы для тестов
├── models/                    # Модели для тестирования
├── audioExamples/             # Примеры аудиофайлов для тестов
├── TestLogger.cs              # Логгер для тестов
└── SpeechRecognition.Tests.csproj # Файл проекта тестов
```

## Чек-листы тестирования

### Чек-лист для обработки аудио

- [ ] Проверка добавления WAV-заголовка к PCM-данным
- [ ] Проверка определения формата аудиофайла
- [ ] Проверка ресемплирования аудио
- [ ] Проверка разделения аудиофайла на фрагменты
- [ ] Проверка подготовки аудиоданных для распознавания
- [ ] Проверка обработки некорректных аудиоданных

### Чек-лист для распознавания речи

- [ ] Проверка распознавания речи из WAV-файла
- [ ] Проверка распознавания речи из PCM-данных
- [ ] Проверка инициализации моделей
- [ ] Проверка корректного освобождения ресурсов
- [ ] Проверка распознавания речи с различными языками
- [ ] Проверка обработки некорректных входных данных

### Чек-лист для управления сессиями

- [ ] Проверка создания сессии
- [ ] Проверка добавления фрагментов в очередь
- [ ] Проверка изменения приоритета фрагментов
- [ ] Проверка отмены обработки фрагментов
- [ ] Проверка паузы и возобновления обработки
- [ ] Проверка очистки очереди
- [ ] Проверка параллельной обработки фрагментов

### Чек-лист для восстановления

- [ ] Проверка повторных попыток при временных сбоях
- [ ] Проверка политики с константной задержкой
- [ ] Проверка политики с экспоненциальной задержкой
- [ ] Проверка обработки исключений
- [ ] Проверка логирования ошибок

## Тестовые кейсы

### Тестовый кейс: Распознавание речи из аудиофайла

**Идентификатор**: TC-001

**Приоритет**: Высокий

**Требования**: R1.1 (Распознавание речи из WAV-файла)

**Модуль**: SpeechRecognitionService

**Содержание**:
1. Создать экземпляр SpeechRecognitionService
2. Инициализировать сервис
3. Вызвать метод ProcessFileAsync с путем к валидному WAV-файлу
4. Проверить возвращаемый результат

**Ожидаемый результат**:
- Метод вернет непустую строку с распознанным текстом
- Событие RecognitionCompleted будет вызвано

### Тестовый кейс: Обработка очереди распознавания

**Идентификатор**: TC-002

**Приоритет**: Средний

**Требования**: R2.3 (Асинхронная обработка фрагментов)

**Модуль**: SessionManager

**Содержание**:
1. Создать экземпляр SpeechRecognitionService
2. Инициализировать сервис
3. Добавить несколько фрагментов в очередь с разными приоритетами
4. Запустить обработку очереди с параллелизмом
5. Проверить результаты обработки и порядок обработки фрагментов

**Ожидаемый результат**:
- Фрагменты обрабатываются в порядке приоритета
- Все фрагменты успешно обработаны
- События QueueItemStatusChanged вызываются для каждого фрагмента

### Тестовый кейс: Восстановление после ошибки

**Идентификатор**: TC-003

**Приоритет**: Высокий

**Требования**: R3.1 (Восстановление после временных сбоев)

**Модуль**: RetryPolicy

**Содержание**:
1. Создать экземпляр SpeechRecognitionService с политикой восстановления
2. Создать условие, вызывающее временный сбой (например, недоступность ресурса)
3. Вызвать метод обработки аудио
4. Проверить, что операция успешно выполнена после нескольких попыток

**Ожидаемый результат**:
- Операция успешно завершается после нескольких попыток
- Логируются сообщения о повторных попытках
- Возвращается корректный результат

## Покрытие кода тестами

Для измерения покрытия кода тестами используется инструмент coverlet. Цель проекта - достичь не менее 80% покрытия кода тестами.

Для запуска тестов с измерением покрытия:

```
dotnet test /p:CollectCoverage=true /p:CoverletOutputFormat=opencover
```

Для анализа покрытия можно использовать инструмент ReportGenerator:

```
reportgenerator -reports:coverage.opencover.xml -targetdir:coverage_report
```

## Руководство по добавлению новых тестов

При добавлении новых функций в систему необходимо также добавлять соответствующие тесты:

1. **Модульные тесты** для проверки функциональности отдельных компонентов
2. **Интеграционные тесты** для проверки взаимодействия компонентов
3. **Тесты восстановления** для проверки обработки ошибок

### Шаблон модульного теста

```csharp
[Fact]
public async Task MethodName_Scenario_ExpectedResult()
{
    // Arrange
    var component = new Component();
    
    // Act
    var result = await component.MethodAsync();
    
    // Assert
    Assert.Equal(expectedValue, result);
}
```

### Шаблон интеграционного теста

```csharp
[Fact]
public async Task ComponentInteraction_Scenario_ExpectedResult()
{
    // Arrange
    var component1 = new Component1();
    var component2 = new Component2(component1);
    
    // Act
    var result = await component2.UseComponent1Async();
    
    // Assert
    Assert.Equal(expectedValue, result);
}
```

## Мок-объекты и заглушки

Для изоляции компонентов при тестировании используются мок-объекты и заглушки:

```csharp
// Пример использования Moq для создания мок-объекта
var mockRecognizer = new Mock<ISpeechRecognizer>();
mockRecognizer.Setup(r => r.RecognizeSpeechAsync(It.IsAny<byte[]>(), It.IsAny<CancellationToken>()))
    .ReturnsAsync("Тестовый результат распознавания");

var service = new SpeechRecognitionService(mockRecognizer.Object, mockAudioProcessor.Object, mockLogger.Object);
```